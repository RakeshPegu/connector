import express from 'express'
const router = express.Router()
router.post('/create_a_subscriptio/:planId',)
router.get('/get_all_subscriptions')
router.get('/get_a_subscription/:subscriptionId')
router.put('/update_a_subscription/:subscriptionId')